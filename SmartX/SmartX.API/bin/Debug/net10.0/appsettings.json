{
  "Mongo": {
    "ConnectionString": "mongodb+srv://bicostzumi_db_user:VerbFSsH3qWNhn1r@cluster0.rxbolvt.mongodb.net/?appName=Cluster0",
    "Database": "SmartX"
  },

  "Firebase": {
    "ProjectId": "smartx-d8206",
    "ServiceAccountPath": ""
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}

/*

i have this WPF and web core API application. Give me setup and basic starting code for this setup. it should have local and online database, maybe using mongoDB. Also uses firebase authentication service. uses shared project for models and DTOs. 

Yes. Since this is quite a large project (around 40-60 files), I'd build it as if we were starting a real production application.

What we'll build
SmartX
│
├── SmartX.API
│      ASP.NET Core Minimal API
│      MongoDB
│      Firebase Authentication
│
├── SmartX.Shared
│      Models
│      DTOs
│      Responses
│
└── SmartX.WPF
       MVVM
       LiteDB
       Firebase Login
       Background Sync
Architecture:

                Firebase
              Authentication
                    │
             Login(email,password)
                    │
          Firebase ID Token (JWT)
                    │
     ┌──────────────┴─────────────┐
     │                            │
     ▼                            ▼
WPF stores token            ASP.NET verifies token

                    │
                    ▼
               MongoDB Atlas
         Users Sensors Telemetry
                    ▲
                    │
              Background Sync
                    ▲
                    │
                LiteDB Cache
Series Plan
Instead of dumping hundreds of lines of code into one response, I'd build it in logical parts.

Part 1 — Shared Library (Foundation)
We'll create:

SmartX.Shared

Models
    Sensor.cs
    User.cs
    TelemetryPacket.cs
    PowerMeter.cs

DTOs
    LoginRequest.cs
    LoginResponse.cs
    SensorRequest.cs
    TelemetryRequest.cs

Responses
    ApiResponse.cs

Enums
Constants
Part 2 — MongoDB
We'll build

MongoContext.cs

Repositories

ISensorRepository
SensorRepository

ITelemetryRepository
TelemetryRepository

IUserRepository
UserRepository
Part 3 — Firebase Authentication
We'll build

FirebaseAuthenticationService

Program.cs

JWT Verification

Authentication Endpoints
Users login with Firebase.

API verifies tokens.

No passwords stored.

Part 4 — API Endpoints
AuthenticationEndpoints

SensorEndpoints

TelemetryEndpoints

UploadEndpoints
Part 5 — LiteDB
LiteDbService

Settings Collection

Telemetry Queue

Cached Sensors

Cached Users
Part 6 — WPF Authentication
Login Window

AuthenticationService

ApiService

Token Storage
Part 7 — Background Sync
Offline

↓

Save to LiteDB

↓

Internet detected

↓

Upload

↓

MongoDB

↓

Remove cache
Part 8 — Sensor Dashboard
Gateway

Sensors

Telemetry

Live Refresh

Charts
Every part will include
For every class I'll provide:

✔ Complete file

✔ Folder location

✔ Namespace

✔ NuGet packages

✔ Program.cs changes

✔ Dependency Injection

✔ Explanation of how it works

For example:

Step 12

Create

SmartX.API
    Data
        MongoContext.cs
followed by the complete code.

Then

Step 13

Create

Repositories
    SensorRepository.cs
with the full implementation, and so on.

I recommend we build it in this order:
Shared project (models, DTOs, ApiResponse)

MongoDB integration (MongoContext + repositories)

Firebase Authentication (client + API verification)

Minimal API endpoints

LiteDB offline cache

WPF MVVM client

Offline sync service

Polish (logging, validation, configuration, deployment)

This approach keeps every step runnable before moving to the next one, making it much easier to understand and debug.


lets strat

Excellent. We'll build this as if we're creating a production application from scratch. I'll make each step compile before moving to the next one.

Phase 1 - Shared Project
This project contains only classes shared between the API and WPF.

No MongoDB.

No LiteDB.

No Firebase.

No HttpClient.

Just models, DTOs and common responses.

Your project should look like this:

SmartX.Shared
│
├── Models
│   ├── Sensor.cs
│   ├── PowerMeter.cs
│   ├── TelemetryPacket.cs
│   └── User.cs
│
├── DTOs
│   ├── LoginRequest.cs
│   ├── LoginResponse.cs
│   ├── SensorRequest.cs
│   └── TelemetryRequest.cs
│
├── Responses
│   └── ApiResponse.cs
│
├── Enums
│
└── Constants
Step 1 - Create ApiResponse
Create

SmartX.Shared
    Responses
        ApiResponse.cs
namespace SmartX.Shared.Responses;

public class ApiResponse<T>
{
    public bool Success { get; init; }

    public string Message { get; init; } = string.Empty;

    public T? Data { get; init; }

    public List<string> Errors { get; init; } = new();
}
Every endpoint in the API will return this.

Example

{
    "success": true,
    "message": "Sensor found",
    "data": {
        ...
    },
    "errors": []
}
Step 2 - Sensor Model
Models
    Sensor.cs
using MongoDB.Bson;
using MongoDB.Bson.Serialization.Attributes;

namespace SmartX.Shared.Models;

public class Sensor
{
    [BsonId]
    [BsonRepresentation(BsonType.ObjectId)]
    public string? Id { get; set; }

    public string Name { get; set; } = string.Empty;

    public string Description { get; set; } = string.Empty;

    public string Location { get; set; } = string.Empty;

    public bool IsActive { get; set; }

    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
}
Why the Mongo Attributes?
Later we'll save directly to MongoDB.

Mongo automatically maps

Id
to

ObjectId
without extra configuration.

Step 3 - PowerMeter
using MongoDB.Bson;
using MongoDB.Bson.Serialization.Attributes;

namespace SmartX.Shared.Models;

public class PowerMeter
{
    [BsonId]
    [BsonRepresentation(BsonType.ObjectId)]
    public string? Id { get; set; }

    public string SensorId { get; set; } = string.Empty;

    public double Voltage { get; set; }

    public double Current { get; set; }

    public double Power => Voltage * Current;
}
Step 4 - TelemetryPacket
using MongoDB.Bson;
using MongoDB.Bson.Serialization.Attributes;

namespace SmartX.Shared.Models;

public class TelemetryPacket
{
    [BsonId]
    [BsonRepresentation(BsonType.ObjectId)]
    public string? Id { get; set; }

    public string SensorId { get; set; } = string.Empty;

    public double Voltage { get; set; }

    public double Current { get; set; }

    public double Temperature { get; set; }

    public DateTime Timestamp { get; set; } = DateTime.UtcNow;
}
Step 5 - User
We're not storing passwords.

Firebase does that.

using MongoDB.Bson;
using MongoDB.Bson.Serialization.Attributes;

namespace SmartX.Shared.Models;

public class User
{
    [BsonId]
    [BsonRepresentation(BsonType.ObjectId)]
    public string? Id { get; set; }

    public string FirebaseUid { get; set; } = string.Empty;

    public string Email { get; set; } = string.Empty;

    public string DisplayName { get; set; } = string.Empty;

    public string Role { get; set; } = "User";

    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
}
Notice there is no Password property.

Step 6 - LoginRequest
namespace SmartX.Shared.DTOs;

public class LoginRequest
{
    public string Email { get; set; } = string.Empty;

    public string Password { get; set; } = string.Empty;
}
Step 7 - LoginResponse
namespace SmartX.Shared.DTOs;

public class LoginResponse
{
    public string IdToken { get; set; } = string.Empty;

    public string RefreshToken { get; set; } = string.Empty;

    public string Email { get; set; } = string.Empty;

    public string DisplayName { get; set; } = string.Empty;
}
This is what the WPF app stores after signing in.

Step 8 - SensorRequest
namespace SmartX.Shared.DTOs;

public class SensorRequest
{
    public string Name { get; set; } = string.Empty;

    public string Description { get; set; } = string.Empty;

    public string Location { get; set; } = string.Empty;
}
Step 9 - TelemetryRequest
namespace SmartX.Shared.DTOs;

public class TelemetryRequest
{
    public string SensorId { get; set; } = string.Empty;

    public double Voltage { get; set; }

    public double Current { get; set; }

    public double Temperature { get; set; }
}
NuGet Packages (Shared)
Install only:

MongoDB.Bson
We only need the BSON attributes here. The full MongoDB.Driver package will be installed in the API project.

End of Phase 1
At this point your solution should compile and you should have:

SmartX.Shared
│
├── Models
│   ├── Sensor
│   ├── User
│   ├── PowerMeter
│   └── TelemetryPacket
│
├── DTOs
│   ├── LoginRequest
│   ├── LoginResponse
│   ├── SensorRequest
│   └── TelemetryRequest
│
└── Responses
    └── ApiResponse<T>
Next: Phase 2 – MongoDB Integration
In the next step we'll:

Create a free MongoDB Atlas cluster.

Configure the connection in appsettings.json.

Add MongoContext.

Build ISensorRepository, ITelemetryRepository, and IUserRepository.

Register everything with dependency injection so your API is ready to read and write data.


Today 11:42 AM
next

Great. Now we'll build the MongoDB layer. After this step, your API will be able to connect to MongoDB Atlas and perform CRUD operations through repositories.

Phase 2 - MongoDB Integration
Step 1 - Create a MongoDB Atlas Database
Go to https://www.mongodb.com/atlas

Create a free account.

Create a Free M0 Cluster.

Create a database user (username/password).

Under Network Access, allow your IP (or 0.0.0.0/0 for development only).

Click Connect → Drivers and copy the connection string.

It will look similar to:

mongodb+srv://smartxadmin:YourPassword@smartx.xxxxx.mongodb.net/?retryWrites=true&w=majority
Step 2 - Install NuGet Packages
In SmartX.API, install:

MongoDB.Driver
Step 3 - appsettings.json
Replace yours with something like:

{
  "Mongo": {
    "ConnectionString": "mongodb+srv://smartxadmin:password@smartx.xxxxx.mongodb.net/",
    "Database": "SmartX"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*"
}
Later we'll move the connection string into User Secrets or environment variables.

Step 4 - Create the Data Folder
SmartX.API
│
├── Data
│     MongoContext.cs
│
├── Repositories
│
└── Interfaces
Step 5 - MongoContext.cs
Data/MongoContext.cs

using MongoDB.Driver;
using SmartX.Shared.Models;

namespace SmartX.API.Data;

public class MongoContext
{
    private readonly IMongoDatabase _database;

    public MongoContext(IConfiguration configuration)
    {
        var client = new MongoClient(
            configuration["Mongo:ConnectionString"]);

        _database = client.GetDatabase(
            configuration["Mongo:Database"]);
    }

    public IMongoCollection<Sensor> Sensors =>
        _database.GetCollection<Sensor>("Sensors");

    public IMongoCollection<TelemetryPacket> Telemetry =>
        _database.GetCollection<TelemetryPacket>("Telemetry");

    public IMongoCollection<User> Users =>
        _database.GetCollection<User>("Users");

    public IMongoCollection<PowerMeter> PowerMeters =>
        _database.GetCollection<PowerMeter>("PowerMeters");
}
This class acts like an EF Core DbContext, but for MongoDB.

Step 6 - Create Repository Interfaces
Create:

Interfaces
│
├── ISensorRepository.cs
├── ITelemetryRepository.cs
└── IUserRepository.cs
ISensorRepository.cs
using SmartX.Shared.Models;

namespace SmartX.API.Interfaces;

public interface ISensorRepository
{
    Task<List<Sensor>> GetAllAsync();

    Task<Sensor?> GetByIdAsync(string id);

    Task CreateAsync(Sensor sensor);

    Task UpdateAsync(Sensor sensor);

    Task DeleteAsync(string id);
}
ITelemetryRepository.cs
using SmartX.Shared.Models;

namespace SmartX.API.Interfaces;

public interface ITelemetryRepository
{
    Task<List<TelemetryPacket>> GetBySensorAsync(string sensorId);

    Task InsertAsync(TelemetryPacket packet);

    Task InsertManyAsync(IEnumerable<TelemetryPacket> packets);
}
IUserRepository.cs
using SmartX.Shared.Models;

namespace SmartX.API.Interfaces;

public interface IUserRepository
{
    Task<User?> GetByFirebaseUidAsync(string uid);

    Task<User?> GetByEmailAsync(string email);

    Task CreateAsync(User user);
}
Step 7 - SensorRepository
Create

Repositories
    SensorRepository.cs
using MongoDB.Driver;
using SmartX.API.Data;
using SmartX.API.Interfaces;
using SmartX.Shared.Models;

namespace SmartX.API.Repositories;

public class SensorRepository : ISensorRepository
{
    private readonly MongoContext _db;

    public SensorRepository(MongoContext db)
    {
        _db = db;
    }

    public async Task<List<Sensor>> GetAllAsync()
    {
        return await _db.Sensors
            .Find(_ => true)
            .ToListAsync();
    }

    public async Task<Sensor?> GetByIdAsync(string id)
    {
        return await _db.Sensors
            .Find(s => s.Id == id)
            .FirstOrDefaultAsync();
    }

    public async Task CreateAsync(Sensor sensor)
    {
        await _db.Sensors.InsertOneAsync(sensor);
    }

    public async Task UpdateAsync(Sensor sensor)
    {
        await _db.Sensors.ReplaceOneAsync(
            s => s.Id == sensor.Id,
            sensor);
    }

    public async Task DeleteAsync(string id)
    {
        await _db.Sensors.DeleteOneAsync(
            s => s.Id == id);
    }
}
Step 8 - TelemetryRepository
using MongoDB.Driver;
using SmartX.API.Data;
using SmartX.API.Interfaces;
using SmartX.Shared.Models;

namespace SmartX.API.Repositories;

public class TelemetryRepository : ITelemetryRepository
{
    private readonly MongoContext _db;

    public TelemetryRepository(MongoContext db)
    {
        _db = db;
    }

    public async Task<List<TelemetryPacket>> GetBySensorAsync(string sensorId)
    {
        return await _db.Telemetry
            .Find(t => t.SensorId == sensorId)
            .SortByDescending(t => t.Timestamp)
            .ToListAsync();
    }

    public async Task InsertAsync(TelemetryPacket packet)
    {
        await _db.Telemetry.InsertOneAsync(packet);
    }

    public async Task InsertManyAsync(IEnumerable<TelemetryPacket> packets)
    {
        await _db.Telemetry.InsertManyAsync(packets);
    }
}
Step 9 - UserRepository
using MongoDB.Driver;
using SmartX.API.Data;
using SmartX.API.Interfaces;
using SmartX.Shared.Models;

namespace SmartX.API.Repositories;

public class UserRepository : IUserRepository
{
    private readonly MongoContext _db;

    public UserRepository(MongoContext db)
    {
        _db = db;
    }

    public async Task<User?> GetByFirebaseUidAsync(string uid)
    {
        return await _db.Users
            .Find(u => u.FirebaseUid == uid)
            .FirstOrDefaultAsync();
    }

    public async Task<User?> GetByEmailAsync(string email)
    {
        return await _db.Users
            .Find(u => u.Email == email)
            .FirstOrDefaultAsync();
    }

    public async Task CreateAsync(User user)
    {
        await _db.Users.InsertOneAsync(user);
    }
}
Step 10 - Register Everything
In Program.cs, add:

using SmartX.API.Data;
using SmartX.API.Interfaces;
using SmartX.API.Repositories;
Then register the services:

builder.Services.AddSingleton<MongoContext>();

builder.Services.AddScoped<ISensorRepository, SensorRepository>();
builder.Services.AddScoped<ITelemetryRepository, TelemetryRepository>();
builder.Services.AddScoped<IUserRepository, UserRepository>();
Folder Structure After Phase 2
Your API project should now look like:

SmartX.API
│
├── Data
│   └── MongoContext.cs
│
├── Interfaces
│   ├── ISensorRepository.cs
│   ├── ITelemetryRepository.cs
│   └── IUserRepository.cs
│
├── Repositories
│   ├── SensorRepository.cs
│   ├── TelemetryRepository.cs
│   └── UserRepository.cs
│
├── Endpoints
├── Services
├── Program.cs
└── appsettings.json
Test the Connection
You can temporarily add this to Program.cs after building the app to verify the connection:

using (var scope = app.Services.CreateScope())
{
    var db = scope.ServiceProvider.GetRequiredService<MongoContext>();

    var count = await db.Sensors.CountDocumentsAsync(_ => true);

    Console.WriteLine($"Connected to MongoDB. Sensors: {count}");
}
If your connection string is correct, the application should start without errors, create the SmartX database automatically on first write, and print the sensor count.

Next Phase: Firebase Authentication
We'll integrate Firebase so that:

The WPF client signs users in with Firebase Authentication.

The client sends the Firebase ID token with every API request.

The API validates the token using the Firebase Admin SDK.

New authenticated users are automatically created (or updated) in the MongoDB Users collection. This gives you secure authentication without storing passwords yourself.



*/

/*
  Phase 3 - Firebase Authentication
Authentication Flow
                WPF Client
                     │
         Email + Password Login
                     │
                     ▼
      Firebase Authentication Server
                     │
         Returns Firebase ID Token (JWT)
                     │
                     ▼
          Store Token in WPF Memory
                     │
Authorization: Bearer eyJhbGciOi...
                     │
                     ▼
             SmartX API
                     │
       Verify Token (Firebase Admin)
                     │
                     ▼
         Read/Create User in MongoDB
                     │
                     ▼
            Execute Requested Action
Step 1 - Create a Firebase Project

Go to:

https://console.firebase.google.com

Create a project
SmartX

Skip Google Analytics.

Enable Email Authentication

Go to

Authentication

↓

Sign-in method

Enable

Email / Password
Step 2 - Generate Service Account

Go to

Project Settings

↓

Service Accounts

↓

Generate New Private Key

You'll download something like

firebase-adminsdk.json

Move it into

SmartX.API

Authentication

firebase-adminsdk.json

Do not commit this file to Git.

Add it to .gitignore:

firebase-adminsdk.json
Step 3 - Install Packages
SmartX.API

Install

FirebaseAdmin

Google.Apis.Auth

Microsoft.AspNetCore.Authentication.JwtBearer
SmartX.WPF

Install

Firebase.Auth

(We'll use the Firebase REST API to sign in.)

Step 4 - API Folder

Create

Authentication

│
├── FirebaseAuthenticationService.cs
├── FirebaseUserContext.cs
└── FirebaseExtensions.cs
Step 5 - Initialize Firebase

In Program.cs

Add

using FirebaseAdmin;
using Google.Apis.Auth.OAuth2;

Before

var app = builder.Build();

Add

FirebaseApp.Create(new AppOptions
{
    Credential = GoogleCredential.FromFile(
        "Authentication/firebase-adminsdk.json")
});

Firebase Admin is now initialized.

Step 6 - FirebaseAuthenticationService

Create

Authentication
    FirebaseAuthenticationService.cs
using FirebaseAdmin.Auth;

namespace SmartX.API.Authentication;

public class FirebaseAuthenticationService
{
    public async Task<FirebaseToken> VerifyTokenAsync(string jwt)
    {
        return await FirebaseAuth.DefaultInstance
            .VerifyIdTokenAsync(jwt);
    }
}

This verifies the JWT received from the WPF client.

Step 7 - Register Service

Program.cs

builder.Services.AddSingleton<FirebaseAuthenticationService>();
Step 8 - Create Authentication Middleware

Instead of repeating token validation in every endpoint, create an extension method.

Create

Authentication

FirebaseExtensions.cs
using FirebaseAdmin.Auth;

namespace SmartX.API.Authentication;

public static class FirebaseExtensions
{
    public static async Task<FirebaseToken?> ValidateFirebaseToken(
        this HttpContext context)
    {
        if (!context.Request.Headers.TryGetValue(
            "Authorization",
            out var value))
        {
            return null;
        }

        var token = value.ToString()
            .Replace("Bearer ", "");

        if (string.IsNullOrWhiteSpace(token))
            return null;

        try
        {
            return await FirebaseAuth.DefaultInstance
                .VerifyIdTokenAsync(token);
        }
        catch
        {
            return null;
        }
    }
}

Now any endpoint can call

var firebaseUser = await http.ValidateFirebaseToken();
Step 9 - Create User Context
Authentication

FirebaseUserContext.cs
namespace SmartX.API.Authentication;

public class FirebaseUserContext
{
    public string Uid { get; set; } = "";

    public string Email { get; set; } = "";

    public string Name { get; set; } = "";
}

We'll expand this later for roles.

Step 10 - Create Authentication Endpoint
Endpoints

AuthenticationEndpoints.cs

Example:

using SmartX.API.Authentication;
using SmartX.API.Interfaces;
using SmartX.Shared.Models;

namespace SmartX.API.Endpoints;

public static class AuthenticationEndpoints
{
    public static IEndpointRouteBuilder MapAuthenticationEndpoints(
        this IEndpointRouteBuilder app)
    {
        app.MapPost("/auth/me",
        async (
            HttpContext context,
            IUserRepository users) =>
        {
            var token = await context.ValidateFirebaseToken();

            if (token == null)
                return Results.Unauthorized();

            var user = await users.GetByFirebaseUidAsync(token.Uid);

            if (user == null)
            {
                user = new User
                {
                    FirebaseUid = token.Uid,
                    Email = token.Claims["email"]?.ToString() ?? "",
                    DisplayName = token.Claims.TryGetValue("name", out var name)
                        ? name?.ToString() ?? ""
                        : ""
                };

                await users.CreateAsync(user);
            }

            return Results.Ok(user);
        });

        return app;
    }
}

The first time a user authenticates:

Firebase verifies the token.
If the user doesn't exist in MongoDB, they're created automatically.
Subsequent logins return the existing user.
Step 11 - Map the Endpoint

In Program.cs:

app.MapAuthenticationEndpoints();
Step 12 - WPF Login Service

We'll use Firebase's Identity Toolkit REST API.

Create:

Services

AuthenticationService.cs
using System.Net.Http.Json;
using SmartX.Shared.DTOs;

namespace SmartX.WPF.Services;

public class AuthenticationService
{
    private readonly HttpClient _http;

    // Replace with your Firebase Web API Key
    private const string ApiKey = "YOUR_FIREBASE_WEB_API_KEY";

    public AuthenticationService(HttpClient http)
    {
        _http = http;
    }

    public async Task<LoginResponse?> LoginAsync(LoginRequest request)
    {
        var url =
            $"https://identitytoolkit.googleapis.com/v1/accounts:signInWithPassword?key={ApiKey}";

        var response = await _http.PostAsJsonAsync(url, new
        {
            email = request.Email,
            password = request.Password,
            returnSecureToken = true
        });

        if (!response.IsSuccessStatusCode)
            return null;

        var json = await response.Content.ReadFromJsonAsync<LoginResponse>();

        return json;
    }
}
Small Change to LoginResponse

Firebase returns additional fields. Update your shared DTO:

public class LoginResponse
{
    public string IdToken { get; set; } = "";

    public string RefreshToken { get; set; } = "";

    public string LocalId { get; set; } = "";

    public string Email { get; set; } = "";

    public string DisplayName { get; set; } = "";

    public int ExpiresIn { get; set; }
}

Note: If you use System.Text.Json, you'll also want to add [JsonPropertyName("idToken")], [JsonPropertyName("refreshToken")], etc., because Firebase's JSON property names don't exactly match C# property names.

What We've Achieved

At this point:

✅ Users log in through Firebase.
✅ WPF receives a Firebase ID token.
✅ WPF includes Authorization: Bearer <token> in API requests.
✅ The API verifies the token using the Firebase Admin SDK.
✅ The user's profile is created or retrieved from the MongoDB Users collection.
Next Phase - WPF Client Infrastructure

Next we'll build the client-side services that tie everything together:

A reusable ApiService that automatically attaches the Firebase token.
Secure token storage.
Dependency injection in the WPF app.
LoginViewModel wired to AuthenticationService.
Navigation to the main application after successful login.
Preparing the client for LiteDB offline caching and synchronization.

*/
/*
  Phase 4 - WPF Infrastructure

Our goal is to end up with this architecture:

Views
   │
ViewModels
   │
ApiService
   │
AuthenticationService
   │
LiteDbService
   │
HTTP API

Everything will be registered through Dependency Injection.

Step 1 - Install NuGet Packages

In SmartX.WPF, install:

CommunityToolkit.Mvvm
Microsoft.Extensions.Hosting
Microsoft.Extensions.DependencyInjection
Microsoft.Extensions.Configuration.Json
LiteDB

We'll add Firebase.Auth later if you decide to use the SDK instead of the REST API.

Step 2 - Create Folder Structure
SmartX.WPF
│
├── Authentication
│
├── Services
│   ├── ApiService.cs
│   ├── AuthenticationService.cs
│   ├── NavigationService.cs
│   ├── TokenStorageService.cs
│   └── LiteDbService.cs
│
├── ViewModels
│
├── Views
│
├── Helpers
│
├── Models
│
├── Resources
│
├── App.xaml
└── App.xaml.cs
Step 3 - Create appsettings.json

Just like ASP.NET, we'll use configuration.

Create

SmartX.WPF
    appsettings.json
{
  "Api": {
    "BaseUrl": "https://localhost:5001/"
  }
}

Set

Copy to Output Directory

↓

Copy if newer
Step 4 - Configure Dependency Injection

Replace App.xaml.cs

using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Hosting;
using SmartX.WPF.Services;
using SmartX.WPF.ViewModels;
using SmartX.WPF.Views;

namespace SmartX.WPF;

public partial class App : Application
{
    public static IHost Host { get; private set; } = null!;

    public App()
    {
        Host = Microsoft.Extensions.Hosting.Host
            .CreateDefaultBuilder()

            .ConfigureAppConfiguration(config =>
            {
                config.AddJsonFile("appsettings.json");
            })

            .ConfigureServices((context, services) =>
            {
                services.AddSingleton<TokenStorageService>();

                services.AddSingleton<ApiService>();

                services.AddSingleton<AuthenticationService>();

                services.AddSingleton<LiteDbService>();

                services.AddSingleton<LoginViewModel>();

                services.AddSingleton<LoginWindow>();
            })

            .Build();
    }

    protected override async void OnStartup(
        StartupEventArgs e)
    {
        await Host.StartAsync();

        var window = Host.Services
            .GetRequiredService<LoginWindow>();

        window.Show();

        base.OnStartup(e);
    }

    protected override async void OnExit(
        ExitEventArgs e)
    {
        await Host.StopAsync();

        Host.Dispose();

        base.OnExit(e);
    }
}

Now the entire WPF application uses dependency injection.

Step 5 - TokenStorageService

Create

Services

TokenStorageService.cs
namespace SmartX.WPF.Services;

public class TokenStorageService
{
    public string? IdToken { get; private set; }

    public string? RefreshToken { get; private set; }

    public bool IsLoggedIn =>
        !string.IsNullOrWhiteSpace(IdToken);

    public void Save(
        string idToken,
        string refreshToken)
    {
        IdToken = idToken;
        RefreshToken = refreshToken;
    }

    public void Clear()
    {
        IdToken = null;
        RefreshToken = null;
    }
}

Later, we'll replace this with encrypted local storage using Windows DPAPI.

Step 6 - ApiService

Create

Services

ApiService.cs
using System.Net.Http.Headers;
using System.Net.Http.Json;

namespace SmartX.WPF.Services;

public class ApiService
{
    private readonly HttpClient _http;
    private readonly TokenStorageService _tokens;

    public ApiService(
        IConfiguration config,
        TokenStorageService tokens)
    {
        _tokens = tokens;

        _http = new HttpClient
        {
            BaseAddress = new Uri(
                config["Api:BaseUrl"]!)
        };
    }

    private void AddToken()
    {
        _http.DefaultRequestHeaders.Authorization = null;

        if (_tokens.IsLoggedIn)
        {
            _http.DefaultRequestHeaders.Authorization =
                new AuthenticationHeaderValue(
                    "Bearer",
                    _tokens.IdToken);
        }
    }

    public async Task<T?> GetAsync<T>(string url)
    {
        AddToken();

        return await _http
            .GetFromJsonAsync<T>(url);
    }

    public async Task<HttpResponseMessage> PostAsync<T>(
        string url,
        T data)
    {
        AddToken();

        return await _http
            .PostAsJsonAsync(url, data);
    }

    public async Task<HttpResponseMessage> PutAsync<T>(
        string url,
        T data)
    {
        AddToken();

        return await _http
            .PutAsJsonAsync(url, data);
    }

    public async Task DeleteAsync(string url)
    {
        AddToken();

        await _http.DeleteAsync(url);
    }
}

Notice that every request automatically sends the Firebase token.

Step 7 - Update AuthenticationService

Instead of only logging into Firebase, save the returned tokens:

public class AuthenticationService
{
    private readonly HttpClient _http;
    private readonly TokenStorageService _storage;

    public AuthenticationService(
        HttpClient http,
        TokenStorageService storage)
    {
        _http = http;
        _storage = storage;
    }

    public async Task<bool> LoginAsync(
        LoginRequest request)
    {
        // Call Firebase REST API...

        // Deserialize LoginResponse

        _storage.Save(
            loginResponse.IdToken,
            loginResponse.RefreshToken);

        return true;
    }

    public void Logout()
    {
        _storage.Clear();
    }
}
Step 8 - LoginViewModel

Using the Community Toolkit:

using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using SmartX.Shared.DTOs;
using SmartX.WPF.Services;

namespace SmartX.WPF.ViewModels;

public partial class LoginViewModel : ObservableObject
{
    private readonly AuthenticationService _auth;

    [ObservableProperty]
    private string email = string.Empty;

    [ObservableProperty]
    private string password = string.Empty;

    public LoginViewModel(AuthenticationService auth)
    {
        _auth = auth;
    }

    [RelayCommand]
    private async Task Login()
    {
        var success = await _auth.LoginAsync(
            new LoginRequest
            {
                Email = Email,
                Password = Password
            });

        if (success)
        {
            MessageBox.Show("Logged in!");
        }
        else
        {
            MessageBox.Show("Login failed.");
        }
    }
}
Step 9 - Register HttpClient

Instead of creating HttpClient manually, let DI manage it.

Update App.xaml.cs:

services.AddHttpClient<AuthenticationService>();

services.AddSingleton<ApiService>();

Even better, you can refactor ApiService to also use IHttpClientFactory, which is the recommended .NET pattern.

Current Architecture
Login Window
      │
LoginViewModel
      │
AuthenticationService
      │
Firebase Login
      │
TokenStorageService
      │
ApiService
      │
ASP.NET Core API
      │
MongoDB

Everything is now loosely coupled and testable.

Before Moving On: One Improvement

At this point, I'd make one architectural improvement before adding LiteDB.

Instead of TokenStorageService only keeping tokens in memory, we should create a SessionService that manages the current authenticated user and tokens together:

public class SessionService
{
    public User? CurrentUser { get; private set; }
    public string? IdToken { get; private set; }
    public string? RefreshToken { get; private set; }

    // SaveSession(), ClearSession(), IsAuthenticated...
}

This avoids having user information scattered across multiple services and makes future features like role-based authorization, automatic token refresh, and restoring sessions after application restart much easier.

Next Phase

We'll build LiteDB offline support, including:

LiteDbService
Local collections (Sensors, TelemetryQueue, Settings)
Repository wrappers
Background synchronization with MongoDB
Conflict handling and upload queue

This is what will make your application work reliably even without an internet connection.


*/

/*
  Awesome. This is the part that makes your application much more professional than a typical CRUD app.

We're going to build an offline-first architecture where:

MongoDB is the source of truth
LiteDB is the local cache
The WPF app continues to work even when the internet is down
A background sync service uploads changes when the connection returns
Phase 5 - LiteDB Offline Cache
Architecture
             MongoDB Atlas
                  ▲
                  │
            Background Sync
                  ▲
                  │
              LiteDB Cache
                  ▲
                  │
          WPF ViewModels

Notice that ViewModels never talk directly to the API.

Everything goes through the local database first.

This makes the UI extremely fast.

Step 1 - Install LiteDB

Install in SmartX.WPF

LiteDB
Step 2 - Create Folder
SmartX.WPF

LocalData
│
├── LiteDbContext.cs
├── LiteDbService.cs
├── Collections
└── Repositories

I like separating local storage from remote services.

Step 3 - LiteDbContext

Create

LocalData/LiteDbContext.cs
using LiteDB;

namespace SmartX.WPF.LocalData;

public class LiteDbContext : IDisposable
{
    private readonly LiteDatabase _database;

    public LiteDbContext()
    {
        _database = new LiteDatabase("SmartX.db");
    }

    public ILiteCollection<SensorCache> Sensors =>
        _database.GetCollection<SensorCache>("Sensors");

    public ILiteCollection<TelemetryQueueItem> TelemetryQueue =>
        _database.GetCollection<TelemetryQueueItem>("TelemetryQueue");

    public ILiteCollection<AppSetting> Settings =>
        _database.GetCollection<AppSetting>("Settings");

    public void Dispose()
    {
        _database.Dispose();
    }
}
Step 4 - Local Models

These are not shared with the API because they contain local-only information.

Create

LocalData/Collections
SensorCache.cs
using LiteDB;
using SmartX.Shared.Models;

namespace SmartX.WPF.LocalData.Collections;

public class SensorCache
{
    [BsonId]
    public string Id { get; set; } = "";

    public Sensor Sensor { get; set; } = new();

    public DateTime LastUpdated { get; set; }
}
TelemetryQueueItem.cs
using LiteDB;
using SmartX.Shared.Models;

namespace SmartX.WPF.LocalData.Collections;

public class TelemetryQueueItem
{
    [BsonId]
    public ObjectId Id { get; set; }

    public TelemetryPacket Packet { get; set; } = new();

    public bool Uploaded { get; set; }

    public DateTime Created { get; set; } =
        DateTime.UtcNow;
}
AppSetting.cs
using LiteDB;

namespace SmartX.WPF.LocalData.Collections;

public class AppSetting
{
    [BsonId]
    public string Key { get; set; } = "";

    public string Value { get; set; } = "";
}
Step 5 - LiteDbService
LocalData/LiteDbService.cs
using LiteDB;
using SmartX.Shared.Models;
using SmartX.WPF.LocalData.Collections;

namespace SmartX.WPF.LocalData;

public class LiteDbService
{
    private readonly LiteDbContext _context;

    public LiteDbService()
    {
        _context = new LiteDbContext();
    }

    // ---------- Sensors ----------

    public List<Sensor> GetSensors()
    {
        return _context.Sensors
            .FindAll()
            .Select(x => x.Sensor)
            .ToList();
    }

    public void SaveSensors(IEnumerable<Sensor> sensors)
    {
        foreach (var sensor in sensors)
        {
            _context.Sensors.Upsert(new SensorCache
            {
                Id = sensor.Id!,
                Sensor = sensor,
                LastUpdated = DateTime.UtcNow
            });
        }
    }

    // ---------- Telemetry Queue ----------

    public void QueueTelemetry(TelemetryPacket packet)
    {
        _context.TelemetryQueue.Insert(
            new TelemetryQueueItem
            {
                Packet = packet
            });
    }

    public List<TelemetryQueueItem> GetPendingTelemetry()
    {
        return _context.TelemetryQueue
            .Find(x => !x.Uploaded)
            .ToList();
    }

    public void MarkUploaded(ObjectId id)
    {
        var item = _context.TelemetryQueue.FindById(id);

        if (item == null)
            return;

        item.Uploaded = true;

        _context.TelemetryQueue.Update(item);
    }

    public void RemoveUploaded()
    {
        _context.TelemetryQueue.DeleteMany(
            x => x.Uploaded);
    }
}
Step 6 - Register LiteDB

In App.xaml.cs

services.AddSingleton<LiteDbService>();

One instance for the whole application.

Step 7 - SyncService

Now the magic.

Services

SyncService.cs
public class SyncService
{
    private readonly ApiService _api;
    private readonly LiteDbService _liteDb;

    public SyncService(
        ApiService api,
        LiteDbService liteDb)
    {
        _api = api;
        _liteDb = liteDb;
    }

    public async Task UploadTelemetryAsync()
    {
        var pending =
            _liteDb.GetPendingTelemetry();

        if (!pending.Any())
            return;

        foreach (var item in pending)
        {
            var response = await _api.PostAsync(
                "/telemetry",
                item.Packet);

            if (response.IsSuccessStatusCode)
            {
                _liteDb.MarkUploaded(item.Id);
            }
        }

        _liteDb.RemoveUploaded();
    }
}
Better Upload Strategy

Instead of this:

POST

Telemetry 1

POST

Telemetry 2

POST

Telemetry 3

send everything together.

POST

/api/upload

[
Telemetry1,

Telemetry2,

Telemetry3
]

This is much faster and scales better.

On the API, your endpoint becomes:

app.MapPost("/api/upload", async (
    List<TelemetryPacket> packets,
    ITelemetryRepository repository) =>
{
    await repository.InsertManyAsync(packets);

    return Results.Ok();
});
Step 8 - Background Worker

Every 30 seconds:

Internet?

↓

YES

↓

Upload Pending Telemetry

↓

Download Latest Sensors

↓

Update LiteDB

↓

Sleep

Example:

public async Task RunAsync()
{
    while (true)
    {
        if (NetworkInterface.GetIsNetworkAvailable())
        {
            await UploadTelemetryAsync();
        }

        await Task.Delay(30000);
    }
}

Note: In a production WPF app, consider implementing this as a BackgroundService registered with the Generic Host rather than an infinite loop started manually. It integrates better with application startup and shutdown.

Data Flow

When a sensor reading is received:

Sensor

↓

TelemetryPacket

↓

LiteDB Queue

↓

UI updates immediately

↓

Background Sync

↓

MongoDB

↓

Delete Queue Item

The user never waits for the network.

One Architectural Improvement

Right now we have:

ViewModel

↓

ApiService

I'd change that to:

ViewModel

↓

SensorService

↓

LiteDB

↓

SyncService

↓

API

Instead of every ViewModel knowing about APIs or LiteDB, create domain services:

Services
│
├── SensorService
├── TelemetryService
├── UserService
└── SyncService

For example, SensorService would:

Read sensors from LiteDB.
Download fresh sensors from the API when online.
Update the local cache.
Return sensors to the UI.

The ViewModel simply calls:

var sensors = await _sensorService.GetSensorsAsync();

It doesn't care whether the data came from LiteDB or MongoDB. This separation keeps your MVVM layer clean, makes unit testing easier, and allows you to change your storage strategy later without touching the UI.

Where We Go Next

We now have the core building blocks:

✅ Shared models and DTOs
✅ MongoDB repositories
✅ Firebase authentication
✅ WPF dependency injection
✅ LiteDB offline cache

The next phase is to implement domain services and Minimal API endpoints (SensorService, TelemetryService, SensorEndpoints, TelemetryEndpoints, etc.), which will complete the end-to-end flow from WPF → LiteDB → API → MongoDB. This is where the application starts functioning as a complete system.

*/

/*
Excellent. At this point, I want to slightly improve the architecture before we continue. The current setup works, but if this is going to be a real-world application, we should introduce a Business Layer (Services) between the API endpoints and the repositories.

The flow becomes:

WPF
   │
ApiService
   │
Minimal API Endpoint
   │
Business Service
   │
Repository
   │
MongoDB

This way:

Endpoints only handle HTTP concerns.
Services contain business rules.
Repositories only talk to MongoDB.

This separation makes testing and future changes much easier.

Phase 6 - Business Services
Step 1 - Create the Service Structure

In SmartX.API, create:

SmartX.API
│
├── Services
│   ├── Interfaces
│   │   ├── ISensorService.cs
│   │   ├── ITelemetryService.cs
│   │   └── IUserService.cs
│   │
│   ├── SensorService.cs
│   ├── TelemetryService.cs
│   └── UserService.cs
Step 2 - ISensorService
using SmartX.Shared.DTOs;
using SmartX.Shared.Models;

namespace SmartX.API.Services.Interfaces;

public interface ISensorService
{
    Task<List<Sensor>> GetAllAsync();

    Task<Sensor?> GetAsync(string id);

    Task<Sensor> CreateAsync(SensorRequest request);

    Task<bool> UpdateAsync(string id, SensorRequest request);

    Task<bool> DeleteAsync(string id);
}
Step 3 - SensorService
using SmartX.API.Interfaces;
using SmartX.API.Services.Interfaces;
using SmartX.Shared.DTOs;
using SmartX.Shared.Models;

namespace SmartX.API.Services;

public class SensorService : ISensorService
{
    private readonly ISensorRepository _repository;

    public SensorService(ISensorRepository repository)
    {
        _repository = repository;
    }

    public async Task<List<Sensor>> GetAllAsync()
    {
        return await _repository.GetAllAsync();
    }

    public async Task<Sensor?> GetAsync(string id)
    {
        return await _repository.GetByIdAsync(id);
    }

    public async Task<Sensor> CreateAsync(SensorRequest request)
    {
        var sensor = new Sensor
        {
            Name = request.Name,
            Description = request.Description,
            Location = request.Location,
            IsActive = true
        };

        await _repository.CreateAsync(sensor);

        return sensor;
    }

    public async Task<bool> UpdateAsync(string id, SensorRequest request)
    {
        var sensor = await _repository.GetByIdAsync(id);

        if (sensor == null)
            return false;

        sensor.Name = request.Name;
        sensor.Description = request.Description;
        sensor.Location = request.Location;

        await _repository.UpdateAsync(sensor);

        return true;
    }

    public async Task<bool> DeleteAsync(string id)
    {
        var sensor = await _repository.GetByIdAsync(id);

        if (sensor == null)
            return false;

        await _repository.DeleteAsync(id);

        return true;
    }
}

Notice how all business logic is here, not in the endpoint.

Step 4 - ITelemetryService
using SmartX.Shared.DTOs;
using SmartX.Shared.Models;

namespace SmartX.API.Services.Interfaces;

public interface ITelemetryService
{
    Task SaveAsync(TelemetryRequest request);

    Task SaveBatchAsync(IEnumerable<TelemetryRequest> requests);

    Task<List<TelemetryPacket>> GetBySensorAsync(string sensorId);
}
Step 5 - TelemetryService
using SmartX.API.Interfaces;
using SmartX.API.Services.Interfaces;
using SmartX.Shared.DTOs;
using SmartX.Shared.Models;

namespace SmartX.API.Services;

public class TelemetryService : ITelemetryService
{
    private readonly ITelemetryRepository _repository;

    public TelemetryService(ITelemetryRepository repository)
    {
        _repository = repository;
    }

    public async Task SaveAsync(TelemetryRequest request)
    {
        var packet = new TelemetryPacket
        {
            SensorId = request.SensorId,
            Voltage = request.Voltage,
            Current = request.Current,
            Temperature = request.Temperature,
            Timestamp = DateTime.UtcNow
        };

        await _repository.InsertAsync(packet);
    }

    public async Task SaveBatchAsync(IEnumerable<TelemetryRequest> requests)
    {
        var packets = requests.Select(r => new TelemetryPacket
        {
            SensorId = r.SensorId,
            Voltage = r.Voltage,
            Current = r.Current,
            Temperature = r.Temperature,
            Timestamp = DateTime.UtcNow
        });

        await _repository.InsertManyAsync(packets);
    }

    public async Task<List<TelemetryPacket>> GetBySensorAsync(string sensorId)
    {
        return await _repository.GetBySensorAsync(sensorId);
    }
}
Step 6 - IUserService
using SmartX.Shared.Models;

namespace SmartX.API.Services.Interfaces;

public interface IUserService
{
    Task<User?> GetOrCreateFirebaseUserAsync(
        string uid,
        string email,
        string displayName);
}
Step 7 - UserService
using SmartX.API.Interfaces;
using SmartX.API.Services.Interfaces;
using SmartX.Shared.Models;

namespace SmartX.API.Services;

public class UserService : IUserService
{
    private readonly IUserRepository _repository;

    public UserService(IUserRepository repository)
    {
        _repository = repository;
    }

    public async Task<User?> GetOrCreateFirebaseUserAsync(
        string uid,
        string email,
        string displayName)
    {
        var user = await _repository.GetByFirebaseUidAsync(uid);

        if (user != null)
            return user;

        user = new User
        {
            FirebaseUid = uid,
            Email = email,
            DisplayName = displayName
        };

        await _repository.CreateAsync(user);

        return user;
    }
}
Step 8 - Register Services

In Program.cs:

using SmartX.API.Services;
using SmartX.API.Services.Interfaces;

Register them:

builder.Services.AddScoped<ISensorService, SensorService>();
builder.Services.AddScoped<ITelemetryService, TelemetryService>();
builder.Services.AddScoped<IUserService, UserService>();
Step 9 - Create Minimal API Endpoints

Now the endpoints become very small.

Create:

Endpoints
├── SensorEndpoints.cs
├── TelemetryEndpoints.cs
└── UserEndpoints.cs
SensorEndpoints.cs
using SmartX.API.Services.Interfaces;
using SmartX.Shared.DTOs;

namespace SmartX.API.Endpoints;

public static class SensorEndpoints
{
    public static IEndpointRouteBuilder MapSensorEndpoints(
        this IEndpointRouteBuilder app)
    {
        var group = app.MapGroup("/api/sensors");

        group.MapGet("/", async (ISensorService service) =>
        {
            return Results.Ok(await service.GetAllAsync());
        });

        group.MapGet("/{id}", async (
            string id,
            ISensorService service) =>
        {
            var sensor = await service.GetAsync(id);

            return sensor is null
                ? Results.NotFound()
                : Results.Ok(sensor);
        });

        group.MapPost("/", async (
            SensorRequest request,
            ISensorService service) =>
        {
            var sensor = await service.CreateAsync(request);

            return Results.Created($"/api/sensors/{sensor.Id}", sensor);
        });

        group.MapPut("/{id}", async (
            string id,
            SensorRequest request,
            ISensorService service) =>
        {
            return await service.UpdateAsync(id, request)
                ? Results.NoContent()
                : Results.NotFound();
        });

        group.MapDelete("/{id}", async (
            string id,
            ISensorService service) =>
        {
            return await service.DeleteAsync(id)
                ? Results.NoContent()
                : Results.NotFound();
        });

        return app;
    }
}

Notice there is no MongoDB code here. The endpoint only coordinates the request and response.

Step 10 - TelemetryEndpoints.cs
using SmartX.API.Services.Interfaces;
using SmartX.Shared.DTOs;

namespace SmartX.API.Endpoints;

public static class TelemetryEndpoints
{
    public static IEndpointRouteBuilder MapTelemetryEndpoints(
        this IEndpointRouteBuilder app)
    {
        var group = app.MapGroup("/api/telemetry");

        group.MapPost("/", async (
            TelemetryRequest request,
            ITelemetryService service) =>
        {
            await service.SaveAsync(request);
            return Results.Ok();
        });

        group.MapPost("/batch", async (
            IEnumerable<TelemetryRequest> requests,
            ITelemetryService service) =>
        {
            await service.SaveBatchAsync(requests);
            return Results.Ok();
        });

        group.MapGet("/{sensorId}", async (
            string sensorId,
            ITelemetryService service) =>
        {
            var data = await service.GetBySensorAsync(sensorId);
            return Results.Ok(data);
        });

        return app;
    }
}
Step 11 - Map the Endpoints

In Program.cs:

app.MapSensorEndpoints();
app.MapTelemetryEndpoints();
app.MapAuthenticationEndpoints();
Your API Flow Is Now
POST /api/telemetry
          │
          ▼
TelemetryEndpoints
          │
          ▼
TelemetryService
          │
          ▼
TelemetryRepository
          │
          ▼
MongoContext
          │
          ▼
MongoDB Atlas

Each layer has a single responsibility:

Endpoints: HTTP routing and responses.
Services: Business rules and mapping DTOs to models.
Repositories: Data access.
MongoContext: Database connection.
Next Phase

The backend is essentially complete. The next phase is to improve the WPF client by introducing client-side repositories and services that mirror the API structure. Instead of ViewModels calling ApiService directly, you'll have:

ViewModel
    │
SensorService (Client)
    │
SensorRepository (Client)
    ├── LiteDB (offline)
    └── API (online)

This creates a clean, symmetrical architecture on both the client and server, making the application easier to maintain and extend.
*/

/*
  Now we get to my favorite part—the client architecture. This is where we'll make your WPF application feel like a professional offline-first desktop application instead of "just calling an API."

One thing I'd change from what we've built so far:

ViewModels should never know whether data comes from LiteDB or the API.

Instead, we'll use client repositories that decide where the data comes from.

Final Client Architecture
Views
    │
ViewModels
    │
Client Services
    │
Repositories
    ├──────────────┐
    │              │
LiteDB         ApiService
    │              │
    └──────┬───────┘
           │
      ASP.NET API
           │
        MongoDB

Notice the similarity to the API.

API

Endpoint
    │
Service
    │
Repository
    │
MongoDB

Both sides now use the same layered architecture.

Phase 7 - Client Repositories
Step 1 - Create Folders
SmartX.WPF
│
├── Repositories
│   ├── Interfaces
│   │   ├── ISensorRepository.cs
│   │   ├── ITelemetryRepository.cs
│   │   └── IUserRepository.cs
│   │
│   ├── SensorRepository.cs
│   ├── TelemetryRepository.cs
│   └── UserRepository.cs
│
├── Services
│   ├── SensorService.cs
│   ├── TelemetryService.cs
│   ├── UserService.cs
│   └── SyncService.cs
Step 2 - ISensorRepository
using SmartX.Shared.Models;

namespace SmartX.WPF.Repositories.Interfaces;

public interface ISensorRepository
{
    Task<List<Sensor>> GetSensorsAsync();

    Task RefreshAsync();

    Task<Sensor?> GetAsync(string id);
}
Step 3 - SensorRepository

This is where the magic happens.

using SmartX.Shared.Models;
using SmartX.WPF.LocalData;
using SmartX.WPF.Services;
using SmartX.WPF.Repositories.Interfaces;

namespace SmartX.WPF.Repositories;

public class SensorRepository : ISensorRepository
{
    private readonly ApiService _api;
    private readonly LiteDbService _liteDb;

    public SensorRepository(
        ApiService api,
        LiteDbService liteDb)
    {
        _api = api;
        _liteDb = liteDb;
    }

    public async Task<List<Sensor>> GetSensorsAsync()
    {
        return _liteDb.GetSensors();
    }

    public async Task RefreshAsync()
    {
        var sensors =
            await _api.GetAsync<List<Sensor>>("/api/sensors");

        if (sensors != null)
        {
            _liteDb.SaveSensors(sensors);
        }
    }

    public async Task<Sensor?> GetAsync(string id)
    {
        return _liteDb
            .GetSensors()
            .FirstOrDefault(x => x.Id == id);
    }
}

Notice something important.

The UI always reads from LiteDB.

It never waits for the internet.

Step 4 - SensorService
using SmartX.Shared.Models;
using SmartX.WPF.Repositories.Interfaces;

namespace SmartX.WPF.Services;

public class SensorService
{
    private readonly ISensorRepository _repository;

    public SensorService(ISensorRepository repository)
    {
        _repository = repository;
    }

    public async Task<List<Sensor>> GetSensorsAsync()
    {
        await _repository.RefreshAsync();

        return await _repository.GetSensorsAsync();
    }

    public Task<Sensor?> GetSensorAsync(string id)
    {
        return _repository.GetAsync(id);
    }
}

Your ViewModel now becomes extremely simple.

Step 5 - TelemetryRepository
using SmartX.Shared.Models;
using SmartX.WPF.LocalData;
using SmartX.WPF.Repositories.Interfaces;

namespace SmartX.WPF.Repositories;

public class TelemetryRepository : ITelemetryRepository
{
    private readonly LiteDbService _liteDb;

    public TelemetryRepository(LiteDbService liteDb)
    {
        _liteDb = liteDb;
    }

    public Task QueueTelemetryAsync(TelemetryPacket packet)
    {
        _liteDb.QueueTelemetry(packet);

        return Task.CompletedTask;
    }
}

Notice there is no API call.

Telemetry is written locally first.

The SyncService uploads it later.

Step 6 - TelemetryService
using SmartX.Shared.DTOs;
using SmartX.Shared.Models;
using SmartX.WPF.Repositories.Interfaces;

namespace SmartX.WPF.Services;

public class TelemetryService
{
    private readonly ITelemetryRepository _repository;

    public TelemetryService(
        ITelemetryRepository repository)
    {
        _repository = repository;
    }

    public async Task SaveAsync(
        TelemetryRequest request)
    {
        var packet = new TelemetryPacket
        {
            SensorId = request.SensorId,
            Voltage = request.Voltage,
            Current = request.Current,
            Temperature = request.Temperature,
            Timestamp = DateTime.UtcNow
        };

        await _repository.QueueTelemetryAsync(packet);
    }
}
Step 7 - Register Everything
builder.Services.AddSingleton<ISensorRepository, SensorRepository>();
builder.Services.AddSingleton<ITelemetryRepository, TelemetryRepository>();

builder.Services.AddSingleton<SensorService>();
builder.Services.AddSingleton<TelemetryService>();

builder.Services.AddSingleton<SyncService>();
Step 8 - DashboardViewModel

Notice how clean this becomes.

public partial class DashboardViewModel : ObservableObject
{
    private readonly SensorService _service;

    [ObservableProperty]
    private List<Sensor> sensors = new();

    public DashboardViewModel(
        SensorService service)
    {
        _service = service;
    }

    [RelayCommand]
    public async Task Load()
    {
        Sensors =
            await _service.GetSensorsAsync();
    }
}

No HttpClient.

No JSON.

No LiteDB.

No Mongo.

The ViewModel only knows about the service.

Offline Flow

Suppose the internet goes down.

Dashboard

↓

SensorService

↓

SensorRepository

↓

LiteDB

↓

Success

The user still sees data.

When telemetry arrives

Telemetry

↓

TelemetryService

↓

TelemetryRepository

↓

LiteDB Queue

No internet required.

When the internet comes back

SyncService

↓

Pending Queue

↓

POST /api/telemetry/batch

↓

MongoDB

↓

Delete Queue

The user never notices.

One Improvement I'd Make

Our current ApiService returns raw models:

var sensors = await _api.GetAsync<List<Sensor>>(...);

Instead, since every API endpoint already returns ApiResponse<T>, let's make the client consume that consistently.

Update ApiService to deserialize:

public async Task<ApiResponse<T>?> GetAsync<T>(string url)
{
    AddToken();
    return await _http.GetFromJsonAsync<ApiResponse<T>>(url);
}

Then the repository becomes:

var response = await _api.GetAsync<List<Sensor>>("/api/sensors");

if (response?.Success == true && response.Data != null)
{
    _liteDb.SaveSensors(response.Data);
}

Now every client request has a standard way to report:

Success/failure
Error messages
Returned data

That consistency pays off as your application grows.

After This

At this point, you have the backbone of a production-ready solution:

✅ Firebase Authentication
✅ MongoDB Atlas repositories
✅ LiteDB offline cache
✅ Repository and service layers on both client and server
✅ Offline synchronization
✅ MVVM with dependency injection

The next enhancements I'd add are production features:

Role-based authorization (Admin, Technician, Viewer) using Firebase custom claims and your Users collection.
Structured logging with Serilog.
FluentValidation for validating DTOs.
Global exception handling and standardized API error responses.
Configuration and secrets management (environment variables, User Secrets, Azure Key Vault if you deploy later).

Those additions turn the application from a solid architecture into one that's ready for deployment and long-term maintenance.
*/

/*
  Phase 8 - Production Features

We'll implement them in this order:

Authentication
        │
Authorization
        │
Validation
        │
Exception Handling
        │
Logging
        │
Health Checks
        │
Background Services
Part 1 - Role-Based Authorization

Currently

Firebase

↓

User Logged In

↓

API verifies token

That's authentication.

Now we'll add authorization.

Example

Admin

Technician

Viewer
User Model

Instead of

public string Role { get; set; }

I'd use an enum.

Create

SmartX.Shared

Enums

UserRole.cs
namespace SmartX.Shared.Enums;

public enum UserRole
{
    Viewer = 0,
    Technician = 1,
    Administrator = 2
}

Update User.cs

public UserRole Role { get; set; }

Now you can't accidentally type

Admin

ADMIN

administrator

Adminstrator

Enums eliminate that problem.

Authorization Service

Create

Services

AuthorizationService.cs
using SmartX.Shared.Enums;

namespace SmartX.API.Services;

public class AuthorizationService
{
    public bool HasRole(
        User user,
        UserRole required)
    {
        return user.Role >= required;
    }
}

Now every endpoint can simply ask

if (!_authorization.HasRole(user, UserRole.Administrator))
    return Results.Forbid();
Phase 2 - Validation

Instead of

Endpoint

↓

Repository

↓

Mongo

we insert validation.

Endpoint

↓

Validator

↓

Service

↓

Repository

Install

FluentValidation

Create

Validators

SensorRequestValidator.cs
using FluentValidation;
using SmartX.Shared.DTOs;

public class SensorRequestValidator
    : AbstractValidator<SensorRequest>
{
    public SensorRequestValidator()
    {
        RuleFor(x => x.Name)
            .NotEmpty()
            .MaximumLength(100);

        RuleFor(x => x.Location)
            .NotEmpty();

        RuleFor(x => x.Description)
            .MaximumLength(500);
    }
}

Now every request is validated before saving.

Phase 3 - Global Exception Middleware

Instead of

try
{
}
catch
{
}

inside every endpoint...

Create

Middleware

ExceptionMiddleware.cs
using SmartX.Shared.Responses;
using System.Text.Json;

namespace SmartX.API.Middleware;

public class ExceptionMiddleware
{
    private readonly RequestDelegate _next;

    public ExceptionMiddleware(RequestDelegate next)
    {
        _next = next;
    }

    public async Task Invoke(HttpContext context)
    {
        try
        {
            await _next(context);
        }
        catch (Exception ex)
        {
            context.Response.StatusCode = 500;

            await context.Response.WriteAsync(
                JsonSerializer.Serialize(
                    new ApiResponse<object>
                    {
                        Success = false,
                        Message = ex.Message
                    }));
        }
    }
}

Program.cs

app.UseMiddleware<ExceptionMiddleware>();

Now every exception is handled consistently.

Phase 4 - Logging

Install

Serilog.AspNetCore

Serilog.Sinks.Console

Serilog.Sinks.File

Program.cs

Log.Logger = new LoggerConfiguration()

.WriteTo.Console()

.WriteTo.File(
    "logs/log-.txt",
    rollingInterval: RollingInterval.Day)

.CreateLogger();

builder.Host.UseSerilog();

Now every request can be logged.

Example

2026-08-03

POST /api/telemetry

User

Voltage

Response Time
Phase 5 - Health Checks

Install

Microsoft.Extensions.Diagnostics.HealthChecks

AspNetCore.HealthChecks.MongoDb

Program.cs

builder.Services
.AddHealthChecks()
.AddMongoDb(
    builder.Configuration["Mongo:ConnectionString"]!);

Map

app.MapHealthChecks("/health");

Visiting

/health

returns

Healthy

or

Unhealthy
Phase 6 - Background Worker

Instead of manually calling Sync...

Create

Services

TelemetrySyncWorker.cs
using Microsoft.Extensions.Hosting;

public class TelemetrySyncWorker
    : BackgroundService
{
    private readonly SyncService _sync;

    public TelemetrySyncWorker(
        SyncService sync)
    {
        _sync = sync;
    }

    protected override async Task ExecuteAsync(
        CancellationToken stoppingToken)
    {
        while (!stoppingToken.IsCancellationRequested)
        {
            await _sync.UploadTelemetryAsync();

            await Task.Delay(
                TimeSpan.FromSeconds(30),
                stoppingToken);
        }
    }
}

Register

builder.Services.AddHostedService<TelemetrySyncWorker>();

Now synchronization is automatic.

Phase 7 - Configuration

Instead of

Firebase.json

Connection Strings

API Keys

inside code...

Use

appsettings.json

↓

appsettings.Development.json

↓

User Secrets

↓

Environment Variables

Production should never contain passwords inside source code.

Phase 8 - API Versioning

Instead of

/api/sensors

use

/api/v1/sensors

Later

/api/v2/sensors

can exist without breaking older clients.

Final Architecture
SmartX.API

Authentication
Authorization
Endpoints
Services
Repositories
Validators
Middleware
Data
Logging
HealthChecks

↓

MongoDB
SmartX.WPF

Views
ViewModels
Services
Repositories
LiteDB
SyncWorker
Authentication

↓

SmartX.API
What I'd Change From Everything We've Built

If I were starting this project today, I'd make one architectural change that will save you a lot of work later.

Separate Contracts from Domain Models

Right now, SmartX.Shared mixes API models, MongoDB persistence attributes, and DTOs. Instead, split them like this:

SmartX.Shared
│
├── Contracts
│   ├── Requests
│   ├── Responses
│   └── DTOs
│
├── Domain
│   ├── Sensor.cs
│   ├── TelemetryPacket.cs
│   ├── User.cs
│   └── Enums
│
└── Common
    ├── ApiResponse.cs
    ├── Constants
    └── Validation

Then, move the MongoDB-specific [BsonId] and [BsonRepresentation] attributes out of the shared domain models and into API persistence models or MongoDB class mappings. This keeps your shared library independent of any database technology, making it easier to reuse with another storage provider or expose through other APIs in the future.

The Next Logical Step

At this stage, the application architecture is essentially complete. Rather than adding more infrastructure, the next valuable step is to implement your actual business features—sensor registration, telemetry ingestion, dashboards, charts, alarms, reporting, and administration—on top of this foundation. The infrastructure we've built is designed to support those features cleanly without major refactoring later.
*/

/*
  At this point, I'd stop adding infrastructure and start building the actual application. The architecture is strong enough that we can now implement features one by one.

For a telemetry/power monitoring system, I'd build the modules in this order:

SmartX
│
├── Authentication
├── Dashboard
├── Sensor Management
├── Gateway Management
├── Live Telemetry
├── Historical Data
├── Charts
├── Alarms
├── Reports
├── Administration
└── Settings
Phase 9 - Dashboard Module

Let's build the first real feature.

Dashboard
┌──────────────────────────────────────────┐
│ SmartX Dashboard                         │
├──────────────────────────────────────────┤
│ Total Sensors      14                    │
│ Online             12                    │
│ Offline             2                    │
│ Alerts              1                    │
├──────────────────────────────────────────┤
│ Recent Telemetry                         │
│ Sensor A      230V    5.4A               │
│ Sensor B      229V    6.1A               │
│ Sensor C      231V    4.8A               │
└──────────────────────────────────────────┘
Step 1 - Create Dashboard DTO

Instead of making many API calls:

GET /Sensors

GET /Telemetry

GET /Users

GET /Alerts

Create one endpoint.

GET /api/dashboard

Create

SmartX.Shared

DTOs

DashboardDto.cs
namespace SmartX.Shared.DTOs;

public class DashboardDto
{
    public int TotalSensors { get; set; }

    public int OnlineSensors { get; set; }

    public int OfflineSensors { get; set; }

    public int AlertCount { get; set; }

    public List<TelemetryPacket> RecentTelemetry { get; set; } = [];
}
Step 2 - Dashboard Service
Services

DashboardService.cs
public interface IDashboardService
{
    Task<DashboardDto> GetDashboardAsync();
}

Implementation:

public class DashboardService : IDashboardService
{
    private readonly ISensorRepository _sensorRepository;
    private readonly ITelemetryRepository _telemetryRepository;

    public DashboardService(
        ISensorRepository sensors,
        ITelemetryRepository telemetry)
    {
        _sensorRepository = sensors;
        _telemetryRepository = telemetry;
    }

    public async Task<DashboardDto> GetDashboardAsync()
    {
        var sensors = await _sensorRepository.GetAllAsync();

        var recent = await _telemetryRepository
            .GetRecentAsync(20);

        return new DashboardDto
        {
            TotalSensors = sensors.Count,
            OnlineSensors = sensors.Count(x => x.IsActive),
            OfflineSensors = sensors.Count(x => !x.IsActive),
            AlertCount = 0, // We'll implement alarms later
            RecentTelemetry = recent
        };
    }
}

We'll need to add GetRecentAsync(int count) to ITelemetryRepository.

Step 3 - Dashboard Endpoint
Endpoints

DashboardEndpoints.cs
public static class DashboardEndpoints
{
    public static IEndpointRouteBuilder
        MapDashboardEndpoints(
            this IEndpointRouteBuilder app)
    {
        var group = app.MapGroup("/api/dashboard");

        group.MapGet("/", async (
            IDashboardService service) =>
        {
            var dashboard =
                await service.GetDashboardAsync();

            return Results.Ok(dashboard);
        });

        return app;
    }
}

Register it:

builder.Services.AddScoped<IDashboardService, DashboardService>();

app.MapDashboardEndpoints();
Step 4 - Dashboard Repository (Client)
public class DashboardRepository
{
    private readonly ApiService _api;

    public DashboardRepository(ApiService api)
    {
        _api = api;
    }

    public async Task<DashboardDto?> GetDashboardAsync()
    {
        var response =
            await _api.GetAsync<DashboardDto>(
                "/api/dashboard");

        return response?.Data;
    }
}
Step 5 - Dashboard Service (Client)
public class DashboardService
{
    private readonly DashboardRepository _repository;

    public DashboardService(
        DashboardRepository repository)
    {
        _repository = repository;
    }

    public Task<DashboardDto?> LoadAsync()
    {
        return _repository.GetDashboardAsync();
    }
}
Step 6 - Dashboard ViewModel
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using SmartX.Shared.DTOs;

public partial class DashboardViewModel
    : ObservableObject
{
    private readonly DashboardService _service;

    [ObservableProperty]
    private DashboardDto? dashboard;

    public DashboardViewModel(
        DashboardService service)
    {
        _service = service;
    }

    [RelayCommand]
    public async Task Load()
    {
        Dashboard = await _service.LoadAsync();
    }
}
Step 7 - Dashboard View
<Grid Margin="20">

    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <UniformGrid Columns="4">

        <GroupBox Header="Sensors">
            <TextBlock
                Text="{Binding Dashboard.TotalSensors}"
                FontSize="28"/>
        </GroupBox>

        <GroupBox Header="Online">
            <TextBlock
                Text="{Binding Dashboard.OnlineSensors}"
                FontSize="28"/>
        </GroupBox>

        <GroupBox Header="Offline">
            <TextBlock
                Text="{Binding Dashboard.OfflineSensors}"
                FontSize="28"/>
        </GroupBox>

        <GroupBox Header="Alerts">
            <TextBlock
                Text="{Binding Dashboard.AlertCount}"
                FontSize="28"/>
        </GroupBox>

    </UniformGrid>

</Grid>
Step 8 - Real-Time Refresh

Instead of requiring the user to press Refresh:

private readonly DispatcherTimer _timer = new();

public DashboardViewModel(...)
{
    _timer.Interval = TimeSpan.FromSeconds(10);

    _timer.Tick += async (_, _) =>
    {
        await Load();
    };

    _timer.Start();
}

The dashboard updates automatically every 10 seconds.

What I'd Build Next

Now that the dashboard is in place, I'd implement the rest of the application in this order:

Sensor Management – Add/edit/delete sensors, assign them to gateways, and manage metadata.
Live Telemetry – Stream or poll recent measurements, display current values, and support filtering by sensor.
Historical Data – Query telemetry by date range with pagination and export options.
Charts – Use a WPF charting library such as LiveCharts2 or OxyPlot to visualize voltage, current, power, and temperature over time.
Alarm Rules – Define thresholds (for example, voltage too high/low, temperature limits) and generate alerts.
Reporting – Export telemetry and summaries to PDF or Excel.
Administration – Manage users, assign roles, and configure application settings.

At this point, you're no longer building infrastructure—you've established a solid foundation and can focus on delivering the features that make SmartX useful.

*/
/*
  Final Architecture (What I'd Build)
SmartX.sln

├── SmartX.API
│
├── SmartX.WPF
│
├── SmartX.Domain
│
├── SmartX.Application
│
├── SmartX.Infrastructure
│
└── SmartX.Shared

Instead of everything living in the API.

Domain

Contains ONLY business objects.

SmartX.Domain

Entities
    Sensor
    Gateway
    Telemetry
    User
    Alarm

Enums

Interfaces

Events

No Mongo.

No Firebase.

No WPF.

No API.

Application

Contains business logic.

Application

Services

Commands

Queries

Interfaces

Validators

Example

CreateSensorCommand

DeleteSensorCommand

UploadTelemetryCommand

GetDashboardQuery
Infrastructure

Contains everything external.

Infrastructure

Mongo

Firebase

LiteDB

Repositories

Email

Logging

If tomorrow you switch MongoDB to SQL Server...

Only Infrastructure changes.

API

Now becomes incredibly small.

Program.cs

Endpoints

Authentication

Middleware

Nothing else.

WPF

Only contains

Views

ViewModels

Navigation

Client Services

No business logic.

Final Data Flow
WPF

↓

Application

↓

Infrastructure

↓

MongoDB

instead of

WPF

↓

API

↓

Repositories

↓

Mongo
I'd also introduce CQRS

Instead of

SensorService

you have

Commands

CreateSensorCommand

UpdateSensorCommand

DeleteSensorCommand

UploadTelemetryCommand

and

Queries

GetDashboard

GetTelemetry

GetSensor

GetUsers

Every class has ONE responsibility.

Then I'd introduce MediatR

Instead of

Endpoint

↓

Service

↓

Repository

the endpoint simply says

await mediator.Send(new CreateSensorCommand(...));

Done.

Logging

Every command automatically logs.

Create Sensor

↓

Log

↓

Execute

↓

Save
Validation

Every command automatically validates.

Command

↓

Validator

↓

Handler

↓

Repository
Caching

Instead of querying Mongo every time

Dashboard

↓

Redis

↓

Mongo

Even if you don't use Redis now, the architecture supports it later.

Background Jobs

Instead of

while(true)
{
}

Use

HostedService

↓

Upload Queue

↓

Mongo
Notifications
Telemetry

↓

Alarm

↓

Email

↓

SMS

↓

Firebase Push

No code changes to telemetry.

Dependency Injection

Your Program.cs becomes tiny.

builder.Services
    .AddApplication()
    .AddInfrastructure(builder.Configuration)
    .AddPresentation();

That's it.

Final Folder Structure
SmartX.API
│
├── Endpoints
├── Middleware
└── Program.cs

SmartX.Application
│
├── Commands
├── Queries
├── Validators
├── Behaviors
├── Interfaces
└── Services

SmartX.Domain
│
├── Entities
├── Enums
├── Events
└── Interfaces

SmartX.Infrastructure
│
├── Mongo
├── Firebase
├── LiteDB
├── Repositories
├── Logging
└── Email

SmartX.WPF
│
├── Views
├── ViewModels
├── Navigation
├── Services
└── Resources

SmartX.Shared
│
├── DTOs
├── Responses
├── Contracts
└── Constants
If this were my project...

I'd actually start over with this architecture rather than continue extending the one we've been building. The concepts we've covered (Firebase, MongoDB, LiteDB, repositories, MVVM, offline sync) are still exactly the same, but organizing them into Domain → Application → Infrastructure from day one will save a lot of refactoring as the project grows.

My Recommendation

Rather than continuing this as individual chat messages, I'd build it as a complete tutorial series where each step produces a working solution:

Part 1: Create the solution and projects.
Part 2: Configure dependency injection and project references.
Part 3: Add MongoDB and repository implementations.
Part 4: Configure Firebase Authentication.
Part 5: Build the WPF login flow.
Part 6: Implement LiteDB offline caching.
Part 7: Add synchronization services.
Part 8: Create the dashboard and sensor management.
Part 9: Implement telemetry, alarms, charts, and reporting.
Part 10: Add testing, logging, deployment, and CI/CD.

That would result in a complete, production-quality SmartX application rather than isolated code snippets, with each part building directly on the previous one.

*/

